---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

const guides = [
  { slug: 'best-restaurants-bay-st-louis', title: 'Best Restaurants in Bay St. Louis', town: 'Bay St. Louis', category: 'Food & Dining' },
  { slug: 'best-seafood-hancock-county', title: 'Best Seafood in Hancock County', town: 'Countywide', category: 'Food & Dining' },
  { slug: 'things-to-do-bay-st-louis', title: 'Things to Do in Bay St. Louis', town: 'Bay St. Louis', category: 'Activities' },
  { slug: 'things-to-do-waveland', title: 'Things to Do in Waveland', town: 'Waveland', category: 'Activities' },
  { slug: 'family-activities-hancock-county', title: 'Family Activities in Hancock County', town: 'Countywide', category: 'Activities' },
  { slug: 'guide-diamondhead', title: "Diamondhead: Hancock County's Planned Community", town: 'Diamondhead', category: 'Town Guide' },
  { slug: 'hidden-gems-hancock-county', title: 'Hidden Gems in Hancock County', town: 'Countywide', category: 'Exploration' },
];

const categories = [...new Set(guides.map(g => g.category))];
---

<Layout
  title="Local Guides — Hancock County MS"
  description="Discover the best of Hancock County, Mississippi with our local guides. Find restaurants, things to do, hidden gems, and more across Bay St. Louis, Waveland, Diamondhead, Kiln, and Pearlington."
>
  <Header activePage="Guides" />

  <section class="hero">
    <div class="container">
      <h1>Local Guides</h1>
      <p class="hero-sub">Insider tips and recommendations for exploring Hancock County</p>
    </div>
  </section>

  <section class="guides-section">
    <div class="container">
      {categories.map(category => (
        <div class="category-group">
          <h2 class="category-title">{category}</h2>
          <div class="guides-grid">
            {guides.filter(g => g.category === category).map(guide => (
              <a href={`/guides/${guide.slug}/`} class="guide-card">
                <span class="guide-town">{guide.town}</span>
                <h3>{guide.title}</h3>
                <span class="read-more">Read guide →</span>
              </a>
            ))}
          </div>
        </div>
      ))}
    </div>
  </section>

  <Footer />
</Layout>

<style>
  .hero {
    background: linear-gradient(135deg, var(--navy) 0%, var(--navy-mid) 100%);
    padding: 8rem 0 4rem;
    text-align: center;
  }

  .hero h1 {
    font-family: var(--font-heading);
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    color: var(--white);
    margin-bottom: 0.5rem;
  }

  .hero-sub {
    color: var(--gray-400);
    font-size: 1.125rem;
  }

  .guides-section {
    padding: 4rem 0;
    background: var(--gray-100);
  }

  .category-group {
    margin-bottom: 3rem;
  }

  .category-group:last-child {
    margin-bottom: 0;
  }

  .category-title {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    color: var(--navy);
    margin-bottom: 1.5rem;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid var(--teal);
    display: inline-block;
  }

  .guides-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1.5rem;
  }

  .guide-card {
    background: var(--white);
    border-radius: var(--radius-md);
    padding: 1.5rem;
    box-shadow: var(--shadow-sm);
    transition: transform 0.2s, box-shadow 0.2s;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .guide-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-md);
  }

  .guide-town {
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--teal);
  }

  .guide-card h3 {
    font-family: var(--font-heading);
    font-size: 1.25rem;
    color: var(--navy);
    line-height: 1.3;
  }

  .read-more {
    margin-top: auto;
    font-size: 0.875rem;
    color: var(--teal);
    font-weight: 600;
  }
</style>
